.env
credentials/
gcs_upload/

# Large source template images at repo root — a copy lives in public/assets/
Add a subheading*.png

# Source data / PII — never committed, only used locally to build gcs_upload/
2025 TEM - CERTIFICATE ISSUE*.xlsx
certificate_manifest.csv
sorted_certificates/
sorted_batch_pdfs/

__pycache__/
*.pyc
.DS_Store
Thumbs.db
